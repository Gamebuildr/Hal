machine:
  environment:
    GOBIN: "/home/ubuntu/bin"

dependencies:
  pre:
    - echo $GCLOUD_SERVICE_KEY | base64 --decode --ignore-garbage > ${HOME}/gcloud-service-key.json
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update
    - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
    - sudo /opt/google-cloud-sdk/bin/gcloud config set project $GCLOUD_PROJECT
    - go get -t -d -v ./...
    - go build -v
    - go install -v

test:
  override:
    - go test -v -race ./...

deployment:
  prod:
    branch: master
    commands:
      - sudo scp -i ~/.ssh/id_circle-ci /home/ubuntu/bin/Hal boomer@35.187.20.26:/var/www/go/bin
      - ssh -i ~/.ssh/id_circle-ci boomer@35.187.20.26 sudo service supervisor restart
      - sudo scp -i ~/.ssh/id_mr-robot /home/ubuntu/bin/Hal chvckd@104.199.104.178:/var/www/go/bin
      - ssh -i ~/.ssh/id_mr-robot chvckd@104.199.104.178 sudo service supervisor restart

